apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ha-storage
  namespace: homeassistant
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 40Gi
  storageClassName: iscsi
