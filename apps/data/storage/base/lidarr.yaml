apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-storage
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: iscsi
  resources:
    requests:
      storage: 2Gi
