apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: change-storage
  namespace: services
spec:
  accessModes:
    - ReadWritMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
