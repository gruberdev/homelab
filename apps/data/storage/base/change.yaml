apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: change-storage
  namespace: services
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: iscsi
  resources:
    requests:
      storage: 1Gi
