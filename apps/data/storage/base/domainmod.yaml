apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: domainmod-storage
  namespace: services
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
