apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drone-data
  namespace: drone
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
