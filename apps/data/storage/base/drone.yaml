apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drone-data
  namespace: drone
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
