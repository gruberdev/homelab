apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monica-storage
  namespace: services
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: iscsi
  resources:
    requests:
      storage: 5Gi
