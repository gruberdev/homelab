apiVersion: v1
kind: Service
metadata:
  name: broadlink-manager-internal
  labels:
    app: broadlink-manager
spec:
  selector:
    app: broadlink-manager
  ports:
    - name: http
      port: 80
      targetPort: 7020
      protocol: TCP
  type: ClusterIP
