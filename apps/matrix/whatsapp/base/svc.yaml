apiVersion: v1
kind: Service
metadata:
  name: whatsapp-svc
  labels:
    app: whatsapp-bridge
spec:
  type: ClusterIP
  selector:
    app: whatsapp-bridge
  ports:
    - name: bridge
      port: 80
      targetPort: 29318
      protocol: TCP
