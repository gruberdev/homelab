apiVersion: v1
kind: Service
metadata:
  name: wallabag-svc
  labels:
    app: wallabag
spec:
  type: ClusterIP
  selector:
    app: wallabag
  ports:
    - name: web
      port: 80
      targetPort: 80
      protocol: TCP
