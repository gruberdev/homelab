---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallabag
  labels:
    app: wallabag
  annotations:
    link.argocd.argoproj.io/external-link: http://wallabag
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: wallabag
  template:
    metadata:
      labels:
        app: wallabag
    spec:
      containers:
        - name: server
          image: wallabag/wallabag:2.5.1
          envFrom:
            - configMapRef:
                name: wallabag-cm
            - secretRef:
                name: wallabag-vars
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 150
            periodSeconds: 10
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 180
            failureThreshold: 3
            periodSeconds: 15
          ports:
            - name: web
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "400m"
              memory: "512Mi"
          volumeMounts:
            - name: media
              mountPath: /var/www/wallabag/web/assets/images
            - name: data
              mountPath: /var/www/wallabag/data
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: wallabag-media
        - name: data
          persistentVolumeClaim:
            claimName: wallabag-storage