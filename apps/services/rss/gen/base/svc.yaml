apiVersion: v1
kind: Service
metadata:
  name: rss-gen
  labels:
    app: rss-gen
spec:
  type: ClusterIP
  selector:
    app: rss-gen
  ports:
    - name: web
      port: 3000
      targetPort: 8080
      protocol: TCP
