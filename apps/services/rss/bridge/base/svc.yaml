apiVersion: v1
kind: Service
metadata:
  name: rss-bridge
  labels:
    app: rss-bridge
spec:
  type: ClusterIP
  selector:
    app: rss-bridge
  ports:
    - name: web
      port: 3002
      targetPort: 80
      protocol: TCP
