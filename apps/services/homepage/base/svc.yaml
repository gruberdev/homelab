apiVersion: v1
kind: Service
metadata:
  name: homepage
  labels:
    app: homepage
  annotations:
    tailscale.com/hostname: "dashboard"
spec:
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: homepage
  type: LoadBalancer
  loadBalancerClass: tailscale
