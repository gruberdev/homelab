apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: archive-cloudflare
spec:
  secretName: archive-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: archive.グルーバー.com
  dnsNames:
    - archive.グルーバー.com
