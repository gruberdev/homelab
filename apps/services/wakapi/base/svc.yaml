apiVersion: v1
kind: Service
metadata:
  name: wakapi
  labels:
    app: wakapi-server
spec:
  type: ClusterIP
  selector:
    app: wakapi-server
  ports:
    - name: web
      port: 80
      targetPort: 3000
      protocol: TCP
