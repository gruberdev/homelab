apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: unifi-internal
  annotations:
    external-dns.alpha.kubernetes.io/hostname: unifi.gr.wtf
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
    external-dns.alpha.kubernetes.io/ttl: "120"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: unifi.gr.wtf
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: unifi-lb
            port:
              name: guest-http
  tls:
  - hosts:
    - unifi.gr.wtf
    secretName: unifi-tls
