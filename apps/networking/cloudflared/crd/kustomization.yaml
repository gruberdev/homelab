apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - https://github.com/adyanth/cloudflare-operator/config/crd
patchesStrategicMerge:
  - overlay/webhook.yaml
  - overlay/webhook-cluster.yaml
  - overlay/cainjection.yaml
  - overlay/cainjection-cluster.yaml

configurations:
- kustomizeconfig.yaml

namespace: kube-system

commonAnnotations:
  reloader.stakater.com/auto: "true"
